<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Followed Requests

    <a data-toggle="collapse" href="#user_followed_requests_new_form">
      Add follower request
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @user.followed_requests.each do |follower_request| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <%= link_to follower_request, follower_request %>

        <%= link_to follower_request, method: :delete, data: { confirm: "Do you really want to delete this follower_request" } do %>
          <i class="fas fa-trash-alt"></i>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="user_followed_requests_new_form">
        <%= render partial: "users/user_followed_requests_new_form" %>
      </div>
    </div>

  </ul>
</div>
